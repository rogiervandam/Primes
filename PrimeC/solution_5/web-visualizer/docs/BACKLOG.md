Prompt

Read @file:docs/AI_MAINTENANCE.md, @file:docs/ARCHTECTURE.md and @file:COMPONENTS.md. Look at the AI_MAINTENANCE.md. Then read @file:docs/BACKLOG.md and select tasks that fit together in one session. Perform these tasks and update the documents with what has been done and remaining work to do.

- ✅ let me turn on the annotations for cachelines also when heatmap is not on. Include "cache" in the text annotation, before the x of delta sign. Move the annotations more to the bottom of the cacheline outline, so that they are more clearly associated with the cacheline and don't overlap with other annotations or visual elements in the grid, providing a clearer and more informative visual representation of the cacheline annotations when analyzing the changes being made to the bit storage in relation to the sieve algorithm. DONE: `CachelineAnnotationsOverlay` no longer gates on `heatMapEnabled`; `Visualizer.jsx` calls `rebuildHeatMap` whenever `cachelineAnnotation !== 'none'`; badge text is now prefixed with "cache " (e.g. `cache ×4 Δ3`); badge is placed in the extension zone below the bit cells (not overlapping them); neutral slate-500 badge colour used when heatmap is off; `LayoutTab` annotation button is now clickable and active without requiring the heatmap; `_renderCachelineOutline` `annotActive` no longer requires `heatMapEnabled`.
- ✅ save changed color to local storage. When the user changes the color settings in the settings panel, save those changes to local storage, so that the next time the user opens the visualizer, their preferred color settings will be automatically applied, providing a more personalized and consistent user experience when analyzing the changes being made to the bit storage in relation to the sieve algorithm. DONE: `colorPreset` and `customColors` are now persisted via `viewPrefs` (`DEFAULT_COLOR_PREFS`, `initialColorPreset`, `initialCustomColors` in `viewPrefs.js`; seeded from `initialPrefs` in `Visualizer.jsx` and included in `writeViewPrefs`).
- ✅ in the details panel, all the section should have the same vertical size. DONE: `.detail-sections` grid changed from `align-items: start` to `align-items: stretch`; `.detail-section-card` made a flex column (`display: flex; flex-direction: column`); `.detail-section-rows` gets `flex: 1` so each card's row content expands to fill the card height, keeping all cards in a grid row the same height.
- ✅ let me click in the legend and when i click on an item that has an associated button in the settings, act as if it was clicked, so that i can easily access and adjust the settings related to a specific item in the legend by simply clicking on that item, providing a more intuitive and seamless user experience when analyzing the changes being made to the bit storage in relation to the sieve algorithm. DONE: `LegendSections` accepts an `onAction(key)` prop; actionable rows (primes overlay, range overlay, multiples overlay, heat map, and all four animation styles) are wrapped in `role="button"` divs with `legend-row--actionable` CSS class (cursor:pointer + hover highlight + ⚙ badge on hover). `SettingsPanel` builds a `legendActions` callback that toggles the associated overlay/setting **and** switches to the relevant tab (Layout for overlays, Animation for animation styles). When called from the floating legend the panel is docked and the sidebar reopens on the correct tab. Both the sidebar Legend tab and the floating legend panel receive `onAction`.
- ✅ Minimap is sometimes ca 10-20px over the left settings panel. DONE: The hardcoded `360` px right-inset was smaller than the actual settings panel on macOS (`388 px`), causing overlap. `Visualizer.jsx` now uses `isMacPlatform ? 388 : 328` for both `r.minimapRightInset` (minimap clearance) and `sideInsetRight` (balloon clamping), matching the CSS rules `.settings-sidebar { width: 328px }` / `.platform-mac .settings-sidebar { width: 388px }`.
- ✅ in the single event widget, we have the timeline fading out during the delay between repeats. Apply that also when waiting for a delay between events. DONE: Removed the `&& singleEventLoopActiveRef.current` guard from all 4 `setDelayPhaseMsRef.current(delayMs)` call sites in `triggerAnimation` (after mask, after sequential/bounce, after `animStyle === 'none'`, and after the ripple/fade/pulse effect). The wipe animation in `StepAnimSliders` now activates for both the single-event repeat delay (`delayBetweenRepeats`) and the all-events inter-event delay (`delayBetweenEvents`). Pause-in-flight (`animationReplayPaused`) freezes the wipe in both contexts because `waitForDelay` respects `globalPausedRef.current`, which is set/cleared in sync with `animationReplayPaused`.
- ✅ In the single event widget, make the upper part, including the description/annotation of the event draggable. DONE: Drag initiation moved from the whole banner div to a new `.step-focus-drag-handle` wrapper that surrounds only the `.step-focus-lines` area (title line, annotation, bits-changed). Added `bannerRef` to the outer banner div; `handleMouseDown` now reads `bannerRef.current` (not `e.currentTarget`) for the `isOverDetailPanel` hit-test. CSS: `cursor: grab` / `cursor: grabbing` moved from `.step-focus-banner` / `.step-focus-banner:active` to the new `.step-focus-drag-handle` / `.step-focus-drag-handle:active` rules. The annotation `onMouseDown stopPropagation` is preserved so clicking the annotation to expand it does not inadvertently start a drag or open the events panel. Context rows and sliders remain non-draggable (they already have their own `onMouseDown stopPropagation`).
- ✅ The single event widget should not be on top of the open detail panel or slide under the events panel. Also, when opening it with the button on the details panel, it gets placed just on top of the details panel. Correct the calculation path to fit it in the bottom left corner, but with a padding (ca 30px) from the detail panel header and events panel. DONE: Both `onShowEventTitle` call sites now compute an initial `dragOffsetY` instead of always using `0`. The CSS anchor is `bottom: 20px` in `07-canvas.css`. **CanvasStage.jsx** (called from the detail panel ▲ button): the detail panel’s `onToggle()` fires immediately after, so the panel always ends in a closed state; fixed offset `dragOffsetY = -(22 + 30 - 20) = -32` places the banner 30 px above the closed header. **Visualizer.jsx** (called from the StepPanel ▲ button): detail panel state is unchanged; offset computed dynamically as `-(totalPanelH + 10)` where `totalPanelH = detailOpen ? detailHeight + 22 : 22` (matching the `detailPad` convention already in the codebase).
- ✅ when the single event timeline is playing, i should be able to use the annotations and groupings outlines. Currently, i can only use the grid view buttons when playing, but i need the buttons in the other sections also. DONE: Root cause was `LayoutOverview` being defined as an inline component inside `LayoutTab`. Each `LayoutTab` re-render (triggered by animation frames propagating through the React tree as `stepScrubProgress` updates) created a new `LayoutOverview` function reference; React saw a new component type and unmounted/remounted the entire subtree — destroying the DOM for Annotation and Grouping-outline buttons between clicks. The Grid view section was outside `LayoutOverview` and worked fine because it uses stable module-level `PreviewOptionButton` references directly. Fix: changed `<LayoutOverview />` (React component invocation) to `{LayoutOverview()}` (direct function call) in `LayoutTab`'s main return. React now integrates the returned JSX as part of `LayoutTab`'s render, not as a child component boundary. `AnnotationButton` and `PreviewOptionButton` have stable module-level references and are reconciled normally — no unmount/remount during animation. `SpacingControl` (still defined inside `LayoutOverview`) continues to be a new function reference per call, but it only wraps the spacing-popover triggers in the Arrangements section, which the user did not report as broken.
- ✅ make it possible to join the floating all events widget and the single event widget into one widget or split them into two separate widgets, so that users can choose the layout that best suits their preferences and analysis needs when working with the visualizer to analyze the changes being made to the bit storage in relation to the sieve algorithm. Use a nice animation for join or split. Start the joining when the user drags one of these widgets to the other. Have a button for the separation. When the event side panel is shown, stop this joint mode and show the single event widget as a separate widget and hide the events widget floater, as its controls are now in the events panel. DONE: New `src/visualizer/JoinedEventsWidget.jsx` renders a single merged widget combining the transport controls (play/pause, timeline, nav, speed) from the all-events floater and the event-title content (annotation, bits changed, nearby events, sliders) from `EventTitleBanner`. `widgetsJoined` boolean state added to `Visualizer.jsx` and persisted via `viewPrefs`. **Join**: dragging the all-events floater onto the banner (or vice versa) detects proximity (40 px hit-padding) and calls `joinWidgets()`; a `merge-target` CSS class highlights the target widget in real time; `detectDropZone` in both `StepPanel` and `EventTitleBanner` returns `'joinWidget'` when the cursor is over the other widget. **Split**: the ⊡ split button in the joined widget's header plays a scale+fade-out `@keyframes joined-widget-split` animation (320 ms) then calls `splitWidgets()`. **Appear animation**: `@keyframes joined-widget-appear` (scale 0.88→1 + fade) runs when the joined widget mounts. **Panel open = split**: `toggleStepsPanel` and `revealCurrentStepInPanel` both call `setWidgetsJoined(false)` when expanding the panel. `JoinedEventsWidget`'s expand-panel button also calls `onSplitWidgets` before `setStepsPanelCollapsed(false)`. **Hide**: the ▼ close button calls `onHideWidget` which splits and hides both sub-widgets. Position is stored in `eventTitleSettings.dragOffsetX/Y` so the banner re-appears at the same location on split. CSS in `src/styles/18-joined-widget.css`; imported in `index.css`.
- ✅ when using masks, the details panel should show the type of mask that was used in the sieve program (e.g. uint64v2). DONE: Added a `maskType` `useMemo` in `DetailPanel.jsx` that derives the label from `step.maskWordBits` and `step.patternSlotCount` → `uint${wordBits}v${slotCount}` (e.g. `uint64v2`). A new "Mask type" row (using the `detail-tag block-tag` chip style) is rendered inside the "Mask pattern & preview" section above the existing "Mask pattern" row; shows `—` when no mask data is present.
- ✅ when the all events floating widget is visible, the play/pause and timeline controls in the top bar should be hidden. Remove the button to show them. On start, don't show the all events floating widget, but show the play/pause and timeline controls in the top bar. DONE: `controlsHidden` is no longer a user-toggled state — it is now a derived value (`stepsPanelCollapsed && !allEventsWidgetHidden`) in `Visualizer.jsx`, so the topbar transport automatically hides whenever the floating all-events widget is showing and re-appears when the widget is not. Removed: the eye/eye-off `toolbar-immersive-toggle` button from `Toolbar.jsx`; the ▼ `toolbar-show-events-widget` re-show button; the `toggleControlsHidden` callback; the H keyboard shortcut; `initialControlsHidden` and its field in `viewPrefs.js`; dead `.toolbar-immersive-toggle` / `.toolbar-show-events-widget` CSS. `toggleStepsPanel` now always calls `setAllEventsWidgetHidden(false)` so collapsing the events panel reliably shows the widget (undoes any previous dock-to-top-bar gesture). On first use (no saved prefs) the events panel is expanded → no widget → topbar controls are visible, which matches the expected default.
- ✅ Rename the step-panel to the events-panel. Try to rename all components, div, css, etc. as well, so that there is no confusion about steps when editing code. DONE: `StepPanel.jsx` → `EventsPanel.jsx`; `06-step-panel.css` → `06-events-panel.css`; `14-step-panel-collapsible.css` → `14-events-panel-collapsible.css`. All CSS classes renamed: `.step-panel` → `.events-panel`, `.step-panel-*` → `.events-panel-*`, `.step-item` → `.event-item`, `.step-list` → `.event-list`, `.step-group` → `.event-group`, `.step-group-*` → `.event-group-*`, `.step-search` → `.event-search`, `.step-filter` → `.event-filter`, `.step-depth-*` → `.event-depth-*`, `.step-agg-*` → `.event-agg-*`, `.step-num/op/changes/prime/text/level-*` → `.event-*`. State and callbacks renamed: `stepsPanelCollapsed` → `eventsPanelCollapsed`, `setStepsPanelCollapsed` → `setEventsPanelCollapsed`, `toggleStepsPanel` → `toggleEventsPanel` in `Visualizer.jsx`, `CanvasStage.jsx`, `Toolbar.jsx`, `EventTitleBanner.jsx`, `JoinedEventsWidget.jsx`. viewPrefs migration: `getInitialViewState()` reads `eventsPanelCollapsed` first; falls back to legacy `stepsPanelCollapsed` for users with saved prefs. Also updated responsive breakpoints in `12-responsive-base.css`, merge-target selector in `18-joined-widget.css`, and the comment in `05-layout.css`. All docs updated (ARCHITECTURE.md, COMPONENTS.md, AI_MAINTENANCE.md).
- ✅ Split `SieveRenderer._drawBitBody` into three focused methods. DONE: `_drawBitBody` is now a 10-line dispatch that calls `_drawBitBodyLowered`, `_drawBitBodyRaised`, or `_drawBitBodyNormal`. The three depth-mode branches (lowered set bit / 3D-box raised bit / flat normal bit) are now independently readable. No logic changes — purely structural. Addresses AI_MAINTENANCE.md §7 code quality item 13.
- ✅Make a gear icon in the top bar to reveal the right settings panel. Remove the floating settings.
- Try to factor out more elements of the Visualizer.
- ✅ When joining the two widgets, have the bottom left of the single event widget stay in place, so the joined widget has the same position as seen from the lower left. DONE: `EventTitleBanner` and `EventsPanel` both capture `bannerEl.getBoundingClientRect()` at the moment of the join gesture and pass it to `onJoinWidgets(bannerRect)`. `Visualizer.jsx` stores this in `joinBannerRect` state and forwards it to `JoinedEventsWidget` as `initialBannerRect`. The widget computes its initial X offset from `bannerRect.left` immediately (widget width is fixed at 520 px). A `useLayoutEffect` with empty deps fires synchronously after first paint, measures the widget's actual `offsetHeight`, and adjusts the Y so the bottom aligns with `bannerRect.bottom`.
- ✅ When i click on the single events widget gear icon, and the animation settings panel is already active, close the settings panel. DONE: `SettingsPanel` gained an `onActiveTabChange` prop and a `changeActiveTab` wrapper that calls `setActiveTab` and fires the callback on every tab switch (button click, `activeTabRequest` effect, and all `legendActions` paths). `Visualizer.jsx` tracks `settingsActiveTab` state seeded from the callback. `openAnimationSettings` now checks `!settingsCollapsed && settingsActiveTab === 'animation'`; if true it collapses the panel (toggle-close) instead of switching to the animation tab.
- Improve the bit balloons: make a clear connection between the rectangle describing the bit and the bit on the grid. The idea is to have a cartoon like connection between the nearest edge of the bit and the corners of the text rectangle with curved lines with a fill between the, forming a soft curved and smooth edges "triangle"
- When the mask animation reache its final stamp, it moves up quite a bit and then moves down again. This looks off and slow. Make it move just a few pixels up and down at the end and take longer animating the travel and shorter for this final stage. 
- When pushing the joined widget to the details panel, also take the all events controle and timeline in the details panel.
- When pushing the joined widget to the left side (events panel), open de detail panel and all events panel. The details panel should have the single event timeline, but not the all events timeline, because that is already in the all events panel. 
- In the details panel, the mask type en mask pattern should be on the right side of the masks, but remain within the mask pattern & preview. 
- In the details panel, the %progres and gear icon should be on the right side of the timeline, just as when in the widget,
- Create some more engaging coloring schemes
- The visualizer items that are related to an overlay or animation should be clickable, to that it acts as a shortcut. When clicking on a bit state, let the user specify the color.
- ✅ When hovering over the joined events widget or single event widget, it shouldn't trigger popups for the underlying bits. DONE: Added `.events-panel-floating-title` and `.joined-events-widget` to the `overOverlay` CSS-selector check in the `pointermove` handler in `Visualizer.jsx`. The handler already suppressed hover-balloon creation when the cursor was over `.step-focus-banner`; the two floating widget root classes were simply missing from the list.
- Add a keyboard-shortcuts help overlay (press `?` to open), listing all keyboard shortcuts grouped by category (playback, navigation, view, overlays). Overlay should be dismissible with Escape or a close button, and accessible with ARIA roles.
- Very important: Try to split the Visualizer and SieveRenderer in more components. 
- ✅ Collapsed event-group counts: when an event group is collapsed, show the count of child events next to the group label (e.g. "×47") so users know how many events are hidden. DONE: Added a `.event-group-count-badge` `<span>` in `EventsPanel.jsx`'s group header, rendered only when `isCollapsed && group.children.length > 1`. The badge shows `×N` with a pill style (rounded border, subdued fg-dim). The existing `.event-group-info` span (right-aligned, shows "N events · +Δ") is suppressed when collapsed since the badge already conveys the count, removing the redundant text. CSS in `src/styles/06-events-panel.css`.
- Performance overlay: toggleable FPS counter and frame-time graph (last 60 frames) rendered in the canvas corner, useful for diagnosing slow renders on large sieves.
- ✅ Copy event description to clipboard: add a small copy-icon button in the single-event widget that copies the current event's title, annotation, and bit changes as plain text. DONE: Added `CopyIcon` to `src/Icons.jsx` (clipboard-copy SVG). Added a `step-focus-copy-btn` button to `EventTitleBanner.jsx` positioned at `top:6px; right:32px` (left of the existing locate-btn). Clicking calls `navigator.clipboard.writeText(banner.title)` and flips to a `✓` indicator for 1.5 s then reverts (local `copied` state + `copyTimeoutRef`). Same copy button added to the header row of `JoinedEventsWidget.jsx` as `joined-widget-btn joined-widget-copy-btn`. CSS for `.step-focus-copy-btn` added to `07-canvas.css`; the joined-widget button inherits the existing `.joined-widget-btn` rule in `18-joined-widget.css`.
- Make more backlog items, be creative! And add as a final line: "Make more backlog items, be creaive!".
- Find two delightful improvements. And add as a final line: "Find two delightful improvements".
- ✅ Create some more engaging coloring schemes. DONE: Added four new color presets to `src/renderer/constants.js` — `neon` (cyan/pink/dark), `ocean` (teal/amber/slate), `sunset` (gold/magenta/plum), `ice` (pale blue/coral/sky). All four are immediately available in the color preset dropdown.
- ✅ In the details panel, the mask type and mask pattern should be on the right side of the masks. DONE: Mask section restructured in `DetailPanel.jsx` to a side-by-side flex layout: the mask preview cell is on the left (`mask-section-preview`) and the "Mask type" + "Mask pattern" rows are on the right (`mask-section-meta`). CSS added to `src/styles/08b-detail-compact.css`.
- ✅ Add a keyboard-shortcuts help overlay (press `?` to open). DONE: New `KeyboardShortcutsOverlay` component in `src/visualizer/KeyboardShortcutsOverlay.jsx` lists all shortcuts grouped by category (Playback, Navigation, View, Help) in an accessible modal (`role="dialog" aria-modal="true"`). Press `?` to open; press Escape or click the backdrop/close button to dismiss. CSS in `src/styles/19-keyboard-shortcuts.css`; `useKeyboardShortcuts` wired with `toggleShortcutsOverlay`; state in `Visualizer.jsx`.
- ✅ Performance overlay: toggleable FPS counter and frame-time graph (last 60 frames) rendered in the canvas corner. DONE: `SieveRenderer.js` records per-frame durations in a 60-entry ring buffer and renders a compact overlay in the top-right corner showing current FPS (colour-coded green/amber/red), average frame time in ms, and a 60-bar histogram with a 60 fps guide line. Toggle button (bar-chart icon) added to the toolbar in `Toolbar.jsx`; `perfOverlayEnabled` state in `Visualizer.jsx` synced to `r.perfOverlayEnabled`.
- Improve the bit balloons: make a clear connection between the rectangle describing the bit and the bit on the grid. The idea is to have a cartoon like connection between the nearest edge of the bit and the corners of the text rectangle with curved lines with a fill between them, forming a soft curved and smooth edges "triangle".
- When the mask animation reaches its final stamp, it moves up quite a bit and then moves down again. This looks off and slow. Make it move just a few pixels up and down at the end and take longer animating the travel and shorter for this final stage. 
- When pushing the joined widget to the details panel, also take the all events controls and timeline in the details panel.
- When pushing the joined widget to the left side (events panel), open the detail panel and all events panel. The details panel should have the single event timeline, but not the all events timeline, because that is already in the all events panel. 
- In the details panel, the %progress and gear icon should be on the right side of the timeline, just as when in the widget.
- The visualizer items that are related to an overlay or animation should be clickable, so that it acts as a shortcut. When clicking on a bit state, let the user specify the color.
- Very important: Try to split the Visualizer and SieveRenderer into more components. 
- Make more backlog items, be creative! And add as a final line: "Make more backlog items, be creative!".
- Find two delightful improvements. And add as a final line: "Find two delightful improvements".